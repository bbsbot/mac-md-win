<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MacMD.Win.Views.ProjectListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="12" Spacing="4">

            <!-- Navigation Filters -->
            <HyperlinkButton Click="OnAllDocumentsClick" Padding="4,6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8A5;" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="All Documents" VerticalAlignment="Center"/>
                </StackPanel>
            </HyperlinkButton>
            <HyperlinkButton Click="OnFavoritesClick" Padding="4,6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE734;" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="Favorites" VerticalAlignment="Center"/>
                </StackPanel>
            </HyperlinkButton>
            <HyperlinkButton Click="OnRecentClick" Padding="4,6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE823;" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="Recent" VerticalAlignment="Center"/>
                </StackPanel>
            </HyperlinkButton>
            <HyperlinkButton Click="OnArchivedClick" Padding="4,6">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE7B8;" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="Archived" VerticalAlignment="Center"/>
                </StackPanel>
            </HyperlinkButton>

            <!-- Projects Header + New button -->
            <Grid Margin="0,16,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Projects"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="+"
                        Click="OnNewProjectClick"
                        FontSize="14" Padding="6,2"
                        ToolTipService.ToolTip="New Project"/>
            </Grid>

            <ListView x:Name="ProjectsList"
                      SelectionChanged="OnProjectSelectionChanged"
                      RightTapped="OnProjectRightTapped"
                      SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,2">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8B7;" FontSize="12" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Tags Header + New button -->
            <Grid Margin="0,16,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Tags"
                           Style="{StaticResource BodyStrongTextBlockStyle}"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="+"
                        Click="OnNewTagClick"
                        FontSize="14" Padding="6,2"
                        ToolTipService.ToolTip="New Tag"/>
            </Grid>

            <ListView x:Name="TagsList"
                      SelectionChanged="OnTagSelectionChanged"
                      RightTapped="OnTagRightTapped"
                      SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="8" Padding="0,2">
                            <Ellipse Width="10" Height="10"
                                     VerticalAlignment="Center">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{Binding Color}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="{Binding Name}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackPanel>
    </ScrollViewer>
</UserControl>
