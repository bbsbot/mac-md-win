<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MacMD.Win.Views.DocumentListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search bar -->
        <TextBox Grid.Row="0" x:Name="SearchBox"
                 PlaceholderText="Search documents..."
                 Margin="0,0,0,8"
                 TextChanged="OnSearchTextChanged"/>

        <TextBlock Grid.Row="1" Text="Documents"
                   Style="{StaticResource SubtitleTextBlockStyle}"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" Margin="0,0,0,8">
            <Button Content="New Document" Click="OnNewDocumentClick"/>
            <Button Content="Delete" Click="OnDeleteDocumentClick"/>
        </StackPanel>

        <ListView Grid.Row="3"
                  x:Name="DocumentsList"
                  SelectionChanged="OnDocumentSelectionChanged"
                  SelectionMode="Single"
                  RightTapped="OnDocumentRightTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="0,6">
                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock FontSize="11"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                       Text="{Binding ModifiedAt, Converter={StaticResource DateConverter}}"/>
                            <TextBlock FontSize="11"
                                       Foreground="{ThemeResource TextFillColorTertiaryBrush}">
                                <Run Text="{Binding WordCount}"/>
                                <Run Text=" words"/>
                            </TextBlock>
                        </StackPanel>
                        <TextBlock Text="{Binding Preview}" FontSize="12"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis"
                                   MaxLines="2" TextWrapping="Wrap"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
